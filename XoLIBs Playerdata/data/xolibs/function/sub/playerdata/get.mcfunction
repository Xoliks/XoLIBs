# saves the currently loaded data to the specified UUID's permanant storage

data remove storage xolibs:playerdata storage
$data modify storage xolibs:playerdata storage set from storage xolibs:playerdata rom.$(UUID0)_$(UUID1)_$(UUID2)_$(UUID3)